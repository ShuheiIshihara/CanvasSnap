# ゲームスクリーンショット撮影アプリ 要件定義書

## 1. プロジェクト概要

### 1.1 目的

WebブラウザでプレイしているゲームのCanvas要素を、ゲームに干渉することなく安全にスクリーンショット撮影できるデスクトップアプリケーションを開発する。

### 1.2 背景

- 既存のスクリーンショットツールでは要件を満たせない
- オンラインゲームのチート対策システムに検知されにくい方式が必要
- ユーザー情報の一部をマスクする必要がある

### 1.3 スコープ

- **本バージョンのスコープ**: 静止画キャプチャ機能のみ
- 動画録画機能は将来フェーズで別途要件定義を行う

### 1.4 制約事項

- **重要**: オンラインゲームであり、運営がチート対策を実施している
- Chrome拡張機能やブラウザへの直接干渉は**禁止**（アカウント停止リスク）
- ゲームのJavaScript環境に一切影響を与えてはならない

### 1.5 安全性方針（ゲームへの非干渉）

本アプリケーションは以下の方針に従い、ゲームへの干渉を回避する：

#### 1.5.1 実施すること

- OS標準の画面キャプチャAPIのみを使用
- 画面全体または指定領域の画像取得のみを行う
- ローカルストレージへの画像保存のみを行う

#### 1.5.2 実施しないこと

- ゲームプロセス・ブラウザプロセスへのコード注入
- メモリの読み取り・書き込み
- ネットワーク通信の傍受・改変
- キー入力・マウス操作のシミュレーション
- ウィンドウハンドルを用いたブラウザ操作
- ゲームのDOM/Canvas要素への直接アクセス

#### 1.5.3 免責事項

- 各ゲームのチート対策仕様は非公開であり、アカウント停止リスクを完全にゼロにすることはできない
- 本アプリケーションの使用にあたっては、対象ゲームの利用規約を遵守すること
- 本アプリケーション使用に起因するアカウント停止等の問題については、ユーザーの責任とする

## 2. 機能要件

### 2.1 コア機能

#### 2.1.1 スクリーンキャプチャ

- **対象**: ユーザーが範囲選択した任意の矩形領域
  - 現時点では1200×720pxのゲームCanvas領域を主なターゲットとして想定
- **方式**: 画面キャプチャAPI（OS標準機能）を使用
  - macOS: `screencapture` コマンド（v1.0）
    - 将来的にパフォーマンス要求に応じてScreenCaptureKitへの移行を検討
  - Windows: BitBltまたはWindows.Graphics.Capture API
- **座標系**: OSの実画素（physical pixel）ベースで管理
- **安全性**: ゲームプロセス・ブラウザプロセスに一切干渉しない

#### 2.1.2 マルチディスプレイ対応

- 仮想スクリーンスペース全体から範囲選択可能
- キャプチャ座標はプライマリディスプレイ左上を原点とする絶対座標で管理
- ディスプレイ構成変更時は、ユーザーに再設定を促す

#### 2.1.3 Retina / HiDPIディスプレイ対応

- macOS Retina環境では、screencaptureコマンドが実画素で出力するため、追加処理は不要
- Windows環境では、DPIスケーリングを考慮した座標変換を実装
- 設定UIでは論理座標を表示し、内部では実画素座標に変換して保存

#### 2.1.4 マスク機能

- ユーザー情報を含む領域を黒塗りマスク
- マスク座標は**キャプチャ領域左上を(0,0)とする相対座標**で管理
- 設定で有効/無効を切り替え可能
- マスク色: 黒（#000000）

#### 2.1.5 ホットキー機能

- グローバルホットキーでキャプチャ実行
- ホットキーは設定画面でカスタマイズ可能
- デフォルト: Cmd+Shift+S（macOS）/ Ctrl+Shift+S（Windows）
- **制約事項**:
  - OS予約ショートカットとの競合時は登録を拒否し、ユーザーに別の組み合わせを選択させる
  - IME状態に関わらず動作すること（IMEオン時でも反応）

#### 2.1.6 ファイル保存

- **ファイル名形式**: `screenshot_yyyyMMdd_HHmmssfff.png`
  - 例: `screenshot_20241127_143025123.png`
- **ファイル名衝突時**: 同名ファイルが存在する場合は `_001`, `_002` などの連番を付与
  - 例: `screenshot_20241127_143025123_001.png`
- **保存形式**: PNG
- **保存先**: ユーザーが設定で指定したフォルダ
- **デフォルト保存先**: ピクチャフォルダ

### 2.2 設定機能

#### 2.2.1 キャプチャ領域設定

- 画面上でドラッグ操作により範囲選択
- 座標情報を保存（X, Y, Width, Height）- 実画素ベース
- 選択UIは全画面半透明オーバーレイ（マルチディスプレイ対応）
- テストキャプチャ機能で動作確認可能
- **注意**: 本アプリは画面座標ベースでキャプチャし、ウィンドウ位置/サイズの変化には追従しない
  - ユーザーはゲームウィンドウを同じ位置に配置する前提

#### 2.2.2 マスク領域設定

- キャプチャ領域内でドラッグ操作により範囲選択
- 座標はキャプチャ領域左上を原点とする相対座標で保存
- チェックボックスで有効/無効切り替え

#### 2.2.3 ホットキー設定

- 修飾キー（Cmd/Ctrl, Shift, Alt）+ 任意のキーの組み合わせ
- 設定画面で変更可能
- 現在のホットキーを表示
- 競合検出時はエラーメッセージを表示

#### 2.2.4 保存先設定

- フォルダ選択ダイアログで指定
- パスを表示
- 書き込み権限チェックを実施

#### 2.2.5 設定の永続化

- JSON形式で設定ファイルに保存
- アプリ起動時に自動読み込み
- **保存場所**:
  - macOS: `~/Library/Application Support/ScreenshotApp/config.json`
  - Windows: `%AppData%\ScreenshotApp\config.json`
- **設定ファイル破損時の挙動**:
  - JSONパースエラー時はデフォルト設定で起動
  - ユーザーに通知し、設定の再構成を促す
  - 破損した設定ファイルは `.backup` 拡張子でリネームして保持

### 2.3 UI要件

#### 2.3.1 メインウィンドウ

- システムトレイ常駐型アプリ
- 設定画面へのアクセス
- アプリ終了機能

#### 2.3.2 設定ウィンドウ

- ホットキー表示・変更
- キャプチャ領域表示・選択
- マスク領域表示・選択・有効/無効切り替え
- 保存先表示・変更
- テストキャプチャボタン
- 設定保存ボタン

#### 2.3.3 範囲選択ウィンドウ

- 全画面表示（マルチディスプレイ対応）
- 半透明背景
- ドラッグで矩形選択
- 選択中は矩形を視覚的に表示（座標・サイズ情報付き）
- ESCキーでキャンセル

#### 2.3.4 通知方針

- **成功時**: OS標準通知で「スクリーンショットを保存しました」と表示
- **軽微なエラー時**: OS標準通知で内容を表示
- **クリティカルなエラー時**（権限不足、保存先アクセス不可など）: ダイアログで明示し、対処方法を案内

## 3. 非機能要件

### 3.1 パフォーマンス

- **リアルタイム性**: ホットキー押下から0.5秒以内にキャプチャ完了
- **メモリ使用量**: アイドル時は最小限（<50MB）
- **CPU使用率**: キャプチャ時以外は0%に近い
- **測定基準環境**:
  - macOS: Apple M1以降 / メモリ8GB以上 / 内蔵SSD
  - Windows: Intel Core i5相当以上 / メモリ8GB以上 / SSD

### 3.2 互換性

- **優先プラットフォーム**: macOS (Monterey 12.0以降)
- **将来対応**: Windows 10/11
- **.NET**: .NET 8.0以降
- **ディスプレイ**: Retina/HiDPI対応、マルチディスプレイ対応

### 3.3 セキュリティ

- **ゲームへの非干渉**: 1.5節の方針に従う
- **権限**: macOSの画面録画権限が必要
- **データ保護**: ローカルストレージのみ、外部送信なし

### 3.4 ユーザビリティ

- **初回セットアップ**: 5分以内で完了
- **日常操作**: ホットキーのみで撮影完了
- **エラーハンドリング**: 2.3.4節の通知方針に従う

### 3.5 保守性

- コードはC#で記述
- プラットフォーム依存部分はインターフェースで抽象化
- 設定ファイルはJSON形式で人間が読める
- 画像処理ライブラリは ImageSharp を正式採用

## 4. 技術仕様

### 4.1 技術スタック

#### 4.1.1 フレームワーク

- **UI**: Avalonia 11.x
- **ランタイム**: .NET 8.0
- **画像処理**: ImageSharp（正式採用）
  - System.Drawing.Common は macOS/.NET 6以降で非推奨のため不使用

#### 4.1.2 主要ライブラリ

- Avalonia.Desktop
- ReactiveUI (MVVM)
- System.Text.Json (設定管理)
- SixLabors.ImageSharp (画像処理)

### 4.2 アーキテクチャ

#### 4.2.1 プロジェクト構造

```
ScreenshotApp/
├── ScreenshotApp/
│   ├── ViewModels/
│   │   ├── MainWindowViewModel.cs
│   │   ├── SettingsViewModel.cs
│   │   └── RegionSelectorViewModel.cs
│   ├── Views/
│   │   ├── MainWindow.axaml
│   │   ├── SettingsWindow.axaml
│   │   └── RegionSelectorWindow.axaml
│   ├── Services/
│   │   ├── IScreenCaptureService.cs
│   │   ├── MacOSScreenCaptureService.cs
│   │   ├── WindowsScreenCaptureService.cs
│   │   ├── IHotkeyService.cs
│   │   ├── MacOSHotkeyService.cs
│   │   ├── WindowsHotkeyService.cs
│   │   ├── INotificationService.cs
│   │   ├── NotificationService.cs
│   │   └── SettingsService.cs
│   ├── Models/
│   │   ├── CaptureSettings.cs
│   │   ├── HotkeyConfig.cs
│   │   └── CapturedImage.cs
│   ├── Helpers/
│   │   ├── DisplayHelper.cs
│   │   └── FileNameHelper.cs
│   └── App.axaml.cs
└── ScreenshotApp.sln
```

#### 4.2.2 デザインパターン

- **MVVM**: View - ViewModel - Model 分離
- **DI**: 依存性注入でサービスを管理
- **Strategy**: プラットフォーム別実装の切り替え

### 4.3 データモデル

#### 4.3.1 CaptureSettings

```csharp
public class CaptureSettings
{
    /// <summary>
    /// キャプチャ領域（実画素ベースの絶対座標）
    /// </summary>
    public CaptureRegion CaptureRegion { get; set; }
    
    /// <summary>
    /// マスク領域（キャプチャ領域左上を原点とする相対座標）
    /// </summary>
    public CaptureRegion? MaskRegion { get; set; }
    
    /// <summary>
    /// マスク機能の有効/無効
    /// </summary>
    public bool EnableMask { get; set; }
    
    /// <summary>
    /// 保存先フォルダパス
    /// </summary>
    public string SaveFolder { get; set; }
    
    /// <summary>
    /// ホットキー設定
    /// </summary>
    public HotkeyConfig Hotkey { get; set; }
}
```

#### 4.3.2 CaptureRegion

```csharp
/// <summary>
/// キャプチャ領域を表す構造体（実画素ベース）
/// </summary>
public struct CaptureRegion
{
    public int X { get; set; }
    public int Y { get; set; }
    public int Width { get; set; }
    public int Height { get; set; }
    
    public bool IsValid => Width > 0 && Height > 0;
}
```

#### 4.3.3 HotkeyConfig

```csharp
public class HotkeyConfig
{
    public ModifierKeys Modifiers { get; set; }
    public Key Key { get; set; }
    
    public string ToDisplayString()
    {
        // "Cmd+Shift+S" などの表示用文字列を生成
    }
}

[Flags]
public enum ModifierKeys
{
    None = 0,
    Command = 1,   // macOS
    Control = 2,   // Windows / macOS
    Alt = 4,
    Shift = 8
}
```

#### 4.3.4 CapturedImage

```csharp
using SixLabors.ImageSharp;
using SixLabors.ImageSharp.PixelFormats;

/// <summary>
/// キャプチャした画像をラップするクラス
/// </summary>
public class CapturedImage : IDisposable
{
    private Image<Rgba32> _image;
    
    public int Width => _image.Width;
    public int Height => _image.Height;
    
    public CapturedImage(Image<Rgba32> image)
    {
        _image = image;
    }
    
    /// <summary>
    /// 指定領域を黒塗りマスク
    /// </summary>
    public void ApplyMask(CaptureRegion region)
    {
        // ImageSharpの描画APIでマスク処理
    }
    
    /// <summary>
    /// PNG形式で保存
    /// </summary>
    public void SaveAsPng(string filePath)
    {
        _image.SaveAsPng(filePath);
    }
    
    public void Dispose()
    {
        _image?.Dispose();
    }
}
```

### 4.4 インターフェース定義

#### 4.4.1 IScreenCaptureService

```csharp
/// <summary>
/// 画面キャプチャサービスのインターフェース
/// </summary>
public interface IScreenCaptureService
{
    /// <summary>
    /// 指定された画面領域をキャプチャ
    /// </summary>
    /// <param name="region">キャプチャ領域（実画素ベース）</param>
    /// <returns>キャプチャした画像</returns>
    Task<CapturedImage> CaptureRegionAsync(CaptureRegion region);
    
    /// <summary>
    /// 画面キャプチャ権限があるかチェック
    /// </summary>
    bool HasPermission();
    
    /// <summary>
    /// 権限設定画面を開く
    /// </summary>
    void OpenPermissionSettings();
}
```

#### 4.4.2 IHotkeyService

```csharp
/// <summary>
/// グローバルホットキーサービスのインターフェース
/// </summary>
public interface IHotkeyService : IDisposable
{
    /// <summary>
    /// グローバルホットキーを登録
    /// </summary>
    /// <param name="config">ホットキー設定</param>
    /// <param name="callback">押下時のコールバック</param>
    /// <returns>登録成功時true、競合時false</returns>
    bool Register(HotkeyConfig config, Action callback);
    
    /// <summary>
    /// ホットキー登録を解除
    /// </summary>
    void Unregister();
    
    /// <summary>
    /// 指定のホットキーが使用可能かチェック
    /// </summary>
    bool IsAvailable(HotkeyConfig config);
}
```

#### 4.4.3 INotificationService

```csharp
/// <summary>
/// 通知サービスのインターフェース
/// </summary>
public interface INotificationService
{
    /// <summary>
    /// 成功通知を表示
    /// </summary>
    void ShowSuccess(string message);
    
    /// <summary>
    /// エラー通知を表示（OS通知）
    /// </summary>
    void ShowError(string message);
    
    /// <summary>
    /// クリティカルエラーをダイアログで表示
    /// </summary>
    Task ShowCriticalErrorAsync(string title, string message, string? actionHint = null);
}
```

### 4.5 プラットフォーム別実装

#### 4.5.1 macOS

**画面キャプチャ（v1.0実装）:**

```bash
# screencaptureコマンド使用
# -x: サウンド無効
# -R: 領域指定（実画素座標）
screencapture -x -R<x>,<y>,<width>,<height> <output_file>
```

- 一時ファイルに出力後、ImageSharpで読み込み
- 一時ファイルは処理完了後に削除

**将来の改善案:**

- ScreenCaptureKitへの移行でプロセス起動オーバーヘッドを削減
- 直接メモリバッファへの出力が可能

**グローバルホットキー:**

- Carbon FrameworkのRegisterEventHotKey使用
- P/Invoke経由で呼び出し

**権限:**

- 画面録画権限（Screen Recording）が必要
- Info.plistに用途を記載
- 権限がない場合はシステム環境設定を開くよう案内

#### 4.5.2 Windows

**画面キャプチャ:**

```csharp
// DPIスケーリングを考慮した座標変換が必要
// Graphics.CopyFromScreen は論理座標で動作するため、
// 実画素座標への変換を行う

public async Task<CapturedImage> CaptureRegionAsync(CaptureRegion region)
{
    // DPIスケーリング係数を取得
    var scaleFactor = GetDpiScaleFactor();
    
    // 論理座標に変換してキャプチャ
    // 出力画像は実画素サイズ
}
```

**グローバルホットキー:**

- Win32 APIのRegisterHotKey使用
- P/Invoke経由で呼び出し

### 4.6 ファイル名生成ロジック

```csharp
public static class FileNameHelper
{
    /// <summary>
    /// 一意のファイル名を生成（衝突時は連番付与）
    /// </summary>
    public static string GenerateUniqueFileName(string folder, DateTime timestamp)
    {
        var baseName = $"screenshot_{timestamp:yyyyMMdd_HHmmssfff}";
        var extension = ".png";
        var filePath = Path.Combine(folder, baseName + extension);
        
        if (!File.Exists(filePath))
            return filePath;
        
        // 衝突時は連番を付与
        for (int i = 1; i <= 999; i++)
        {
            filePath = Path.Combine(folder, $"{baseName}_{i:D3}{extension}");
            if (!File.Exists(filePath))
                return filePath;
        }
        
        throw new IOException("ファイル名の生成に失敗しました");
    }
}
```

### 4.7 エラーハンドリング

#### 4.7.1 想定エラーと対応

| エラー種別 | 深刻度 | 通知方法 | 対応 |
|-----------|--------|----------|------|
| 画面キャプチャ権限がない | クリティカル | ダイアログ | システム設定を開くボタンを表示 |
| 保存先フォルダにアクセスできない | クリティカル | ダイアログ | 保存先の再設定を促す |
| ディスク容量不足 | クリティカル | ダイアログ | 空き容量の確保を案内 |
| ホットキーが競合 | 通常 | OS通知 | 別のキーを設定するよう案内 |
| キャプチャ領域が未設定 | 通常 | OS通知 | 設定画面を開く |
| 設定ファイル破損 | 通常 | OS通知 | デフォルト設定で起動、再設定を促す |

## 5. 開発計画

### 5.1 フェーズ1: macOS版開発

1. Avaloniaプロジェクト作成
2. 基本UI実装（設定画面、範囲選択UI）
3. macOS向けScreenCaptureService実装（screencaptureコマンド）
4. macOS向けHotkeyService実装
5. 設定の永続化
6. 通知機能実装
7. テスト・デバッグ

### 5.2 フェーズ2: Windows版開発

1. Windows向けScreenCaptureService実装
2. Windows向けHotkeyService実装
3. DPIスケーリング対応
4. クロスプラットフォーム動作確認
5. テスト・デバッグ

### 5.3 フェーズ3: 改善

- プレビュー機能
- 連続撮影モード（オプション）
- ScreenCaptureKitへの移行検討（macOS）

## 6. テスト要件

### 6.1 機能テスト

- [ ] キャプチャ領域選択が正確に動作する
- [ ] マルチディスプレイ環境で範囲選択が正しく動作する
- [ ] Retina/HiDPI環境でキャプチャ座標が正しい
- [ ] マスク機能が正確に動作する（相対座標で正しい位置にマスク）
- [ ] ホットキーが正常に動作する
- [ ] IMEオン時でもホットキーが反応する
- [ ] 設定が正しく保存・読み込みされる
- [ ] 設定ファイル破損時にデフォルト設定で起動する
- [ ] ファイル名が仕様通り生成される
- [ ] ファイル名衝突時に連番が付与される

### 6.2 安全性テスト

- [ ] 想定ターゲットゲームで本アプリ利用による問題事象（BAN/警告/強制終了）が発生しないことを確認
  - 検証方法: 一定期間（2週間程度）、一定回数（100回以上）の利用で問題が発生しないことを確認
- [ ] ゲームのパフォーマンスに影響しない（FPS低下が観測されない）
- [ ] ブラウザプロセスに影響しない

### 6.3 パフォーマンステスト

- [ ] キャプチャ完了まで0.5秒以内（基準環境にて）
- [ ] アイドル時のメモリ使用量が50MB以下
- [ ] 連続撮影（10回）時の安定性

### 6.4 ユーザビリティテスト

- [ ] 初回セットアップが5分以内で完了
- [ ] 範囲選択が直感的に操作可能
- [ ] エラーメッセージが分かりやすく、対処方法が明確

### 6.5 エッジケーステスト

- [ ] ディスプレイ構成変更後の動作
- [ ] 保存先フォルダが削除された場合の動作
- [ ] 権限が取り消された場合の動作

## 7. リリース基準

### 7.1 必須機能

- [x] キャプチャ機能
- [x] マスク機能
- [x] ホットキー設定
- [x] 範囲選択UI
- [x] 保存先設定
- [x] 通知機能

### 7.2 品質基準

- すべての機能テストが通過
- 安全性テストで問題事象が発生していない
- クラッシュや重大なバグがない

### 7.3 ドキュメント

- README（インストール方法、使い方）
- 設定ガイド
- トラブルシューティング
- **重要**: 利用上の注意（ゲーム利用規約遵守の案内、免責事項）

## 8. 将来の拡張案（オプション）

- 撮影履歴表示
- 画像プレビュー機能
- クラウドストレージ連携
- 自動アップロード機能
- 画像編集機能（トリミング、注釈）
- 複数プロファイル対応（複数ゲーム対応）
- 動画録画機能（別途要件定義が必要）

## 9. 参考情報

### 9.1 技術リソース

- Avalonia公式ドキュメント: https://docs.avaloniaui.net/
- .NET公式ドキュメント: https://learn.microsoft.com/dotnet/
- ImageSharp: https://docs.sixlabors.com/

### 9.2 類似アプリケーション（参考）

- Snagit（商用、高機能）
- ShareX（Windows専用、オープンソース）
- Kap（macOS専用、動画録画）

---

## 変更履歴

| バージョン | 日付 | 変更内容 |
|-----------|------|----------|
| 1.0 | 2024-11-27 | 初版作成 |
| 1.1 | 2025-11-27 | レビュー指摘事項を反映 |

### v1.1 主な変更点

1. **安全性方針の明確化**
   - 「ゲームへの非干渉100%保証」を方針ベースの記載に変更
   - 免責事項を追加

2. **座標系・ディスプレイ関連の仕様追加**
   - マルチディスプレイ対応方針を明記
   - Retina/HiDPI対応方針を明記
   - マスク座標を相対座標に変更
   - ウィンドウ追従しない旨を明記

3. **技術仕様の整備**
   - 画像型をBitmapからImageSharpベースに変更
   - CapturedImageラッパークラスを追加
   - インターフェースを非同期対応に修正

4. **運用仕様の追加**
   - ファイル名衝突時の連番付与ルールを追加
   - 設定ファイル保存場所を明記
   - 設定ファイル破損時の挙動を定義
   - 通知方針を明確化

5. **テスト要件の現実化**
   - 「検知されない」を「問題事象が発生しない」に変更
   - パフォーマンス測定基準環境を追加

---

**作成者**: Shuhei
